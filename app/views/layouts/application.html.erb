<!DOCTYPE html>
<html>
  <head>
    <title>EcommerceTest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="sticky-top">
      <nav class="navbar navbar-expand-lg navbar-light bg-light header">
        <div class="container-fluid">
          <h3 class="ms-1">Eコマース計測用サイト</h3>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarScroll">
              <ul class="navbar-nav mr-auto">
                <li class="ms-3"><%= link_to("商品一覧", "/products/index", class: "nav-link") %></li>
                <li class="ms-3"><%= link_to("新規登録", "/users/new", data: {"turbo"=>false}, class: "nav-link") %></li>
                <li class="ms-3"><%= link_to "ログイン", users_sign_in_path, data: {"turbo"=>false}, class: "nav-link" %></li>
                <% if current_user? %> 
                <%#= @user.name %> 
                <li class="ms-3"><%= link_to("ログアウト", "/users/logout", class: "nav-link") %></li>
                <li class="ms-3"><%= link_to("ユーザー詳細", '/users/show', data: {"turbo"=>false}, class: "nav-link") %></li>
            <% end %>
              </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
